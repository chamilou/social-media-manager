// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;
/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ["en", "es", "fr"], // Define supported languages
    defaultLocale: "en", // Set default language
  },
};

export default nextConfig;
